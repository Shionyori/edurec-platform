server:
  port: 8080
  mode: debug

database:
  driver: mysql
  host: localhost
  port: 3306
  username: root
  password: root
  database: edurec
  charset: utf8mb4
  max_idle_conns: 10
  max_open_conns: 100

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0

jwt:
  access_secret: "change-me-in-production"
  refresh_secret: "change-me-in-production-too"
  access_expire: 15
  refresh_expire: 10080

engine:
  # edurec-engine 输出的推荐结果 JSON 路径（路线 B 导入用）
  recommendations_file: "/mnt/d/Project/edurec-engine/model/recommendations.json"